# Спецификация: TUI Testing Infrastructure для rahzom

## Назначение

rahzom — кроссплатформенная утилита синхронизации папок с TUI интерфейсом (Ratatui + crossterm). Для обеспечения качества необходимо тестировать не только логику (unit-тесты), но и сам пользовательский интерфейс на разных платформах.

Данная инфраструктура позволяет Claude Code:
- Автоматически тестировать TUI на Linux и Windows
- Имитировать действия пользователя (навигация, выбор, ввод)
- Проверять визуальный результат через скриншоты
- Манипулировать файловой системой для создания тестовых сценариев

## Общий workflow тестирования

```
┌─────────────────────────────────────────────────────────────────┐
│                    Типичный сценарий теста                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. ИНИЦИАЛИЗАЦИЯ (sandbox-*-init)                              │
│     └─► Убедиться, что тестовая среда готова                    │
│         (VM/контейнер запущен, rahzom собран)                   │
│                                                                  │
│  2. ПОДГОТОВКА ДАННЫХ (sandbox-*-testing)                       │
│     └─► Создать структуру папок для сценария                    │
│         Например: left/doc.txt + right/doc.txt (разное          │
│         содержимое) для теста конфликтов                        │
│                                                                  │
│  3. ЗАПУСК ПРИЛОЖЕНИЯ                                           │
│     └─► Запустить rahzom в терминале sandbox'а                  │
│                                                                  │
│  4. ВЫПОЛНЕНИЕ ДЕЙСТВИЙ                                         │
│     └─► Отправить клавиши: навигация, Enter, Escape...          │
│     └─► Получить скриншот, проверить результат                  │
│                                                                  │
│  5. МОДИФИКАЦИЯ (опционально)                                   │
│     └─► Изменить файлы в фоне                                   │
│     └─► Повторить анализ, проверить реакцию UI                  │
│                                                                  │
│  6. ЗАВЕРШЕНИЕ                                                  │
│     └─► Закрыть приложение (q или Ctrl+C)                       │
│     └─► Опционально: откат к чистому состоянию                  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Архитектура: Linux Docker

```
┌─────────────────────────────────────────────────────────────────┐
│                      HOST (любая ОС)                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────────┐
│  │                     Claude Code                               │
│  │                                                               │
│  │   bash ─────────────────────────┐                            │
│  │                                  │                            │
│  │   docker exec ... ◄─────────────┼────────────────┐           │
│  │                                  │                │           │
│  └──────────────────────────────────┼────────────────┼───────────┘
│                                     │                │
│  ┌──────────────────────────────────┼────────────────┼───────────┐
│  │              Docker Container    │                │           │
│  │              (rahzom-test)       │                │           │
│  │                                  ▼                │           │
│  │   ┌─────────────────────────────────────────┐    │           │
│  │   │              tmux session               │    │           │
│  │   │                                         │    │           │
│  │   │   ┌─────────────────────────────────┐   │    │           │
│  │   │   │         rahzom (TUI)            │   │    │           │
│  │   │   │                                 │   │    │           │
│  │   │   │   ┌─────────────────────────┐   │   │    │           │
│  │   │   │   │  Projects       [+]     │   │   │    │           │
│  │   │   │   │  > Test Project         │   │   │    │           │
│  │   │   │   │    Another Project      │   │   │    │           │
│  │   │   │   └─────────────────────────┘   │   │    │           │
│  │   │   │                                 │   │    │           │
│  │   │   └─────────────────────────────────┘   │    │           │
│  │   │                                         │    │           │
│  │   └─────────────────────────────────────────┘    │           │
│  │                                                   │           │
│  │   Команды управления:                            │           │
│  │   • tmux send-keys -t rahzom 'j'      ◄──────────┘           │
│  │   • tmux send-keys -t rahzom 'Enter'              (клавиши) │
│  │   • tmux capture-pane -t rahzom -p     ──────────► текст    │
│  │   • ansi2html | wkhtmltoimage          ──────────► PNG      │
│  │                                                               │
│  │   Файловая система:                                          │
│  │   /test/left/    ◄──── тестовые папки (создаются скриптом)  │
│  │   /test/right/                                               │
│  │   /app/rahzom/   ◄──── исходники (volume mount)             │
│  │                                                               │
│  └───────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘

Ключевые команды:
─────────────────
# Запуск контейнера
docker run -d --name rahzom-test -v $(pwd):/app/rahzom rahzom-test-image

# Отправка клавиши
docker exec rahzom-test tmux send-keys -t main 'j'

# Получение содержимого экрана (текст с ANSI)
docker exec rahzom-test tmux capture-pane -t main -p -e

# Создание скриншота (вариант с ansi2html)
docker exec rahzom-test sh -c 'tmux capture-pane -t main -p -e | ansi2html > /tmp/screen.html'
docker exec rahzom-test wkhtmltoimage /tmp/screen.html /tmp/screen.png
docker cp rahzom-test:/tmp/screen.png ./screenshot.png

# Работа с файлами
docker exec rahzom-test mkdir -p /test/left /test/right
docker exec rahzom-test sh -c 'echo "content" > /test/left/file.txt'
```

---

## Архитектура: Windows Hyper-V

```
┌─────────────────────────────────────────────────────────────────┐
│                      HOST (Windows с Hyper-V)                    │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────────┐
│  │                     Claude Code                               │
│  │                                                               │
│  │   PowerShell ───────────────────┐                            │
│  │                                  │                            │
│  │   Invoke-Command -VMName ... ◄──┼────────────────┐           │
│  │   (PowerShell Direct)            │                │           │
│  │                                  │                │           │
│  └──────────────────────────────────┼────────────────┼───────────┘
│                                     │                │
│  ┌──────────────────────────────────┼────────────────┼───────────┐
│  │              Hyper-V VM          │                │           │
│  │              (RahzomTest)        │                │           │
│  │              Windows Eval        │                │           │
│  │                                  ▼                │           │
│  │   ┌─────────────────────────────────────────┐    │           │
│  │   │          Windows-MCP Server             │    │           │
│  │   │          (работает в фоне)              │    │           │
│  │   │                                         │    │           │
│  │   │   Tools:                                │    │           │
│  │   │   • State-Tool  → скриншот + UI tree   │    │           │
│  │   │   • Key-Tool    → отправка клавиш      │◄───┘           │
│  │   │   • Click-Tool  → клик мышью           │    (команды)   │
│  │   │                                         │                │
│  │   └────────────────────┬────────────────────┘                │
│  │                        │                                      │
│  │   ┌────────────────────▼────────────────────┐                │
│  │   │         Windows Terminal                 │                │
│  │   │                                         │                │
│  │   │   ┌─────────────────────────────────┐   │                │
│  │   │   │         rahzom.exe (TUI)        │   │                │
│  │   │   │                                 │   │                │
│  │   │   │   ┌─────────────────────────┐   │   │                │
│  │   │   │   │  Projects       [+]     │   │   │                │
│  │   │   │   │  > Test Project         │   │   │                │
│  │   │   │   │    Another Project      │   │   │                │
│  │   │   │   └─────────────────────────┘   │   │                │
│  │   │   │                                 │   │                │
│  │   │   └─────────────────────────────────┘   │                │
│  │   │                                         │                │
│  │   └─────────────────────────────────────────┘                │
│  │                                                               │
│  │   Файловая система:                                          │
│  │   C:\test\left\    ◄──── тестовые папки                     │
│  │   C:\test\right\                                             │
│  │   C:\rahzom\       ◄──── исходники + бинарник               │
│  │                                                               │
│  │   Snapshots:                                                 │
│  │   • "CleanState" ◄──── откат перед каждым тестом            │
│  │                                                               │
│  └───────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘

Ключевые команды:
─────────────────
# Учётные данные VM (сохранить один раз)
$Cred = Get-Credential
$Cred | Export-Clixml C:\secure\vm-creds.xml

# Откат к чистому состоянию
Restore-VMCheckpoint -VMName RahzomTest -Name "CleanState" -Confirm:$false
Start-VM -Name RahzomTest

# Выполнение команды внутри VM (PowerShell Direct)
Invoke-Command -VMName RahzomTest -Credential $Cred -ScriptBlock {
    # Создание тестовых папок
    New-Item -ItemType Directory -Force -Path C:\test\left, C:\test\right
    Set-Content -Path C:\test\left\file.txt -Value "content"
    
    # Запуск Windows-MCP (если не запущен)
    Start-Process -NoNewWindow powershell -ArgumentList "cd C:\Windows-MCP; uv run main.py"
    
    # Запуск rahzom
    Start-Process wt -ArgumentList "C:\rahzom\target\release\rahzom.exe"
}

# Копирование файлов в/из VM
$Session = New-PSSession -VMName RahzomTest -Credential $Cred
Copy-Item -ToSession $Session -Path .\testdata\ -Destination C:\test\ -Recurse
Copy-Item -FromSession $Session -Path C:\screenshots\screen.png -Destination .\
Remove-PSSession $Session

# Управление через Windows-MCP (вызов из PowerShell Direct)
Invoke-Command -VMName RahzomTest -Credential $Cred -ScriptBlock {
    # Windows-MCP слушает на localhost, вызываем его инструменты
    # Это делается через HTTP/WebSocket или напрямую через Python
    python -c "from windows_mcp import key_tool; key_tool('j')"
}
```

---

## Структура скиллов

```
rahzom/
└── skills/
    ├── sandbox-linux-init/
    │   └── SKILL.md
    ├── sandbox-linux-testing/
    │   └── SKILL.md
    ├── sandbox-windows-init/
    │   └── SKILL.md
    └── sandbox-windows-testing/
        └── SKILL.md
```

---

## Скилл: sandbox-linux-init

### Назначение
Инициализация Docker-контейнера для TUI-тестирования rahzom на Linux.

### Что должен уметь
1. Проверить, существует ли контейнер `rahzom-test`
2. Если нет — создать из образа (или собрать образ)
3. Установить зависимости: Rust, tmux, ansi2html, wkhtmltoimage
4. Собрать rahzom (`cargo build --release`)
5. Создать tmux сессию
6. Вернуть статус готовности

### Требования к образу
- Base: `rust:latest` или `ubuntu:22.04`
- Packages: tmux, ansi2html (pip), wkhtmltopdf
- Volume: исходники rahzom монтируются в `/app/rahzom`
- Working dir: `/test` для тестовых данных

### Результат
Готовый контейнер с:
- Собранным rahzom
- Запущенной tmux сессией `main`
- Пустыми папками `/test/left` и `/test/right`

---

## Скилл: sandbox-linux-testing

### Назначение
Выполнение TUI-тестов в Linux Docker контейнере.

### Входные параметры
- **Сценарий теста** (текстовое описание, что проверяем)
- **Структура данных** (опционально, если нужна специфичная)

### Что должен уметь
1. Подготовить тестовые данные согласно сценарию
2. Запустить rahzom в tmux
3. Выполнить последовательность клавиш
4. Делать скриншоты (текст или PNG)
5. Читать/модифицировать файлы в процессе
6. Завершить тест, вернуть результаты

### Команды управления
```bash
# Клавиши навигации
tmux send-keys -t main 'j'        # вниз
tmux send-keys -t main 'k'        # вверх
tmux send-keys -t main 'Enter'    # выбор
tmux send-keys -t main 'Escape'   # назад
tmux send-keys -t main 'q'        # выход

# Скриншот (текст с ANSI)
tmux capture-pane -t main -p -e

# Скриншот (PNG через конвертацию)
tmux capture-pane -t main -p -e | ansi2html > /tmp/s.html
wkhtmltoimage /tmp/s.html /tmp/s.png
```

### Пресеты тестовых данных

**basic_sync** — базовая синхронизация:
```
left/               right/
├── file1.txt       ├── file1.txt (идентичен)
├── file2.txt       └── file3.txt
└── subdir/
    └── nested.txt
```

**conflict** — конфликт изменений:
```
left/               right/
└── doc.txt         └── doc.txt
    "version A"         "version B"
    (mtime: T+1)        (mtime: T+2)
```

**long_names** — длинные имена файлов:
```
left/
└── this_is_a_very_long_filename_that_might_cause_display_issues_in_terminal.txt
```

**unicode** — Unicode в именах:
```
left/
├── документ.txt
├── 文档.txt
└── 📁folder/
    └── 🎉file.txt
```

**large_tree** — много файлов:
```
left/
├── dir001/file001.txt ... file100.txt
├── dir002/...
└── dir100/...
```

---

## Скилл: sandbox-windows-init

### Назначение
Инициализация Hyper-V VM для TUI-тестирования rahzom на Windows.

### Что должен уметь
1. Проверить, существует ли VM `RahzomTest`
2. Если нет — создать VM:
   - Скачать Windows Eval ISO (или использовать существующий)
   - Создать VM с нужными параметрами
   - Установить Windows (может потребовать ручного шага)
   - Установить: Rust, uv, Windows-MCP, Windows Terminal
   - Создать snapshot "CleanState"
3. Если VM существует — запустить, проверить готовность
4. Вернуть статус

### Параметры VM
- Name: `RahzomTest`
- RAM: 4GB (dynamic: 2-8GB)
- CPU: 2 cores
- Disk: 60GB VHD
- Network: Default Switch (для доступа к интернету при setup)

### Установка ПО в VM
```powershell
# Rust
Invoke-WebRequest -Uri 'https://win.rustup.rs/x86_64' -OutFile rustup-init.exe
.\rustup-init.exe -y --default-toolchain stable

# uv (для Windows-MCP)
pip install uv

# Windows-MCP
git clone https://github.com/CursorTouch/Windows-MCP.git C:\Windows-MCP
cd C:\Windows-MCP
uv sync

# Сборка rahzom
cd C:\rahzom
cargo build --release
```

### Результат
- VM запущена
- rahzom собран
- Windows-MCP готов к запуску
- Snapshot "CleanState" существует

---

## Скилл: sandbox-windows-testing

### Назначение
Выполнение TUI-тестов в Windows Hyper-V VM.

### Входные параметры
- **Сценарий теста** (текстовое описание)
- **Структура данных** (опционально)

### Что должен уметь
1. Откатить VM к snapshot "CleanState"
2. Подготовить тестовые данные
3. Запустить Windows-MCP (если не запущен)
4. Запустить rahzom в Windows Terminal
5. Выполнить команды через Windows-MCP:
   - Key-Tool для клавиш
   - State-Tool для скриншотов
6. Читать/модифицировать файлы через PowerShell Direct
7. Вернуть результаты (скриншот)

### Пример выполнения теста
```powershell
$Cred = Import-Clixml C:\secure\vm-creds.xml

# 1. Откат к чистому состоянию
Restore-VMCheckpoint -VMName RahzomTest -Name "CleanState" -Confirm:$false
Start-VM -Name RahzomTest
Start-Sleep -Seconds 30  # ждём загрузки

# 2. Подготовка данных
Invoke-Command -VMName RahzomTest -Credential $Cred -ScriptBlock {
    New-Item -ItemType Directory -Force -Path C:\test\left, C:\test\right
    Set-Content C:\test\left\doc.txt -Value "Version A"
    Set-Content C:\test\right\doc.txt -Value "Version B"
}

# 3. Запуск приложений
Invoke-Command -VMName RahzomTest -Credential $Cred -ScriptBlock {
    Start-Process powershell -ArgumentList "cd C:\Windows-MCP; uv run main.py" -WindowStyle Hidden
    Start-Sleep -Seconds 5
    Start-Process wt -ArgumentList "-M", "C:\rahzom\target\release\rahzom.exe"
}

# 4. Выполнение теста (через Windows-MCP API или напрямую)
# ... отправка клавиш, получение скриншотов ...

# 5. Получение скриншота
$Session = New-PSSession -VMName RahzomTest -Credential $Cred
Copy-Item -FromSession $Session -Path C:\screenshots\screen.png -Destination .\result.png
Remove-PSSession $Session
```

### Пресеты данных
Аналогичны Linux (basic_sync, conflict, long_names, unicode, large_tree).

---

## Формат сценария теста

При вызове testing-скилла передаётся текстовое описание:

```markdown
## Сценарий: Проверка отображения конфликта

### Подготовка
- Создать файл `doc.txt` в left/ с содержимым "Version A"
- Создать файл `doc.txt` в right/ с содержимым "Version B"
- Убедиться, что mtime файлов разный

### Действия
1. Запустить rahzom
2. Создать новый проект с путями left/ и right/
3. Выполнить Analyze (Enter на проекте, затем 'a')
4. Перейти в Preview

### Ожидаемый результат
- В Preview виден файл `doc.txt`
- Статус файла: Conflict (красный цвет или специальная иконка)
- Доступны действия разрешения конфликта
```

Claude Code адаптирует пресеты и действия под конкретный сценарий.

---

## Открытые вопросы

### 1. Скриншоты в Linux
tmux capture-pane возвращает текст с ANSI-кодами. Варианты получения PNG:

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| ansi2html + wkhtmltoimage | Простая установка | Может отличаться от реального вида |
| VHS (charmbracelet/vhs) | Красивый результат | Сложнее настроить |
| termshot | Специализированный инструмент | Нужен X11/framebuffer |
| Текст с ANSI | Самый простой | Claude Code должен "понимать" ANSI |

**Рекомендация**: Начать с текста + ANSI, если недостаточно — добавить ansi2html конвертацию.

### 2. Автоматическая установка Windows
Установка Windows в VM может потребовать ручных шагов (OOBE, настройки). Варианты:
- Использовать unattend.xml для автоматизации
- Один раз установить вручную, затем использовать snapshot
- Документировать ручные шаги в скилле

**Рекомендация**: Один раз установить вручную, создать snapshot "Installed", от него делать "CleanState".

### 3. Синхронизация Windows-MCP и PowerShell Direct
Windows-MCP работает как отдельный процесс. Нужно решить, как Claude Code будет вызывать его инструменты:
- Через WebSocket (если есть сетевой доступ к VM)
- Через PowerShell Direct + Python вызовы
- Через файловый IPC (команды в файл, результаты в файл)

**Рекомендация**: PowerShell Direct + локальные Python вызовы внутри VM.

---

## Референсы

- **rahzom**: https://github.com/serge-sotnyk/rahzom
- **Windows-MCP**: https://github.com/CursorTouch/Windows-MCP
- **Hyper-V PowerShell Direct**: https://learn.microsoft.com/en-us/windows-server/virtualization/hyper-v/manage/manage-windows-virtual-machines-with-powershell-direct
- **Windows Eval VMs**: https://developer.microsoft.com/en-us/windows/downloads/virtual-machines/
- **tmux**: https://github.com/tmux/tmux
- **ansi2html**: https://pypi.org/project/ansi2html/
